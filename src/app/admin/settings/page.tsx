import { Card, CardContent, CardHeader } from "@/components/ui/Card";

export default function AdminSettingsPage() {
  return (
    <Card>
      <CardHeader>
        <div className="text-xs tracking-[0.25em] text-white/60">SETTINGS</div>
      </CardHeader>
      <CardContent>
        <div className="text-sm text-white/60">Settings placeholder.</div>
      </CardContent>
    </Card>
  );
}
